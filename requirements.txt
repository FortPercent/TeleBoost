# Plain pip deps for TeleBoost. Install via:
#   pip install -r requirements.txt
#
# Note: `verl` itself is NOT listed here. It is pinned to volcengine/verl@v0.4.0
# but must be installed with `--no-deps` to avoid pip upgrading torch/cuda/ray.
# See INSTALL.md or requirements-verl.txt for the verl install line.

accelerate
codetiming
datasets
dill
flash-attn
hydra-core
liger-kernel
numpy
pandas
peft
pyarrow>=19.0.0
pybind11
pylatexenc
pre-commit
ray[default]
tensordict<=0.6.2
torchdata
transformers
wandb
packaging>=20.0
uvicorn
fastapi
