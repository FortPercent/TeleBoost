# requirements.txt records the full set of dependencies for development.
#
# X3 stage 2: pin upstream verl. The verl/ tree in this repo only contains the
# 103 files that actually diverge from this upstream pin (see git history at
# stage 2 commit). Stage 3 will move those diffs into teleboost.patches.* so
# the verl/ overlay can be deleted entirely.
verl @ git+https://github.com/volcengine/verl.git@v0.4.0

accelerate
codetiming
datasets
dill
flash-attn
hydra-core
liger-kernel
numpy
pandas
peft
pyarrow>=19.0.0
pybind11
pylatexenc
pre-commit
ray[default]
tensordict<=0.6.2
torchdata
transformers
# vllm==0.8.4
wandb
packaging>=20.0
uvicorn
fastapi
